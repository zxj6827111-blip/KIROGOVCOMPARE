# P4 专家评审问答

## 1. 系统和人工检查是什么关系？

系统用于自动解析、结构化、勾稽检查、比对和提示风险，作用是提高发现问题和定位问题的效率。人工检查仍然是最终确认环节。

系统结论不应直接替代业务人员签字。尤其是 `UNCERTAIN`、`NOT_ASSESSABLE`、缺表、跨页、表头不足、长表格截断等场景，应由人工结合原始年报和业务资料复核。

## 2. 解析错误如何处理？

解析错误会通过任务中心、报告详情、勾稽检查或问题项暴露出来。处理步骤建议为：

1. 在 `/jobs` 查看解析任务状态和失败原因。
2. 在报告详情查看结构化数据和检查结果。
3. 对失败或不确定项查看证据说明。
4. 判断是样本质量、格式异常、解析能力边界还是环境问题。
5. 需要修复时按问题清单进入后续阶段，不在验收现场直接改代码或数据库。

如果样本属于缺表、表头不足、跨页、长表格尾部截断等复杂情况，应记录风险并进行人工复核。

## 3. 数据质量风险如何解释？

数据质量风险是系统对解析和比对结果可信度的提示，不是对单位业务结果的定责结论。

当前 P4-2 已把数据质量和证据链展示增强到前端可见层面，包括字段路径、原始值、解析值、比对值、来源线索和风险原因。没有详细来源时，系统应保守提示，不伪造页码、坐标、OCR 原文或人工判断。

专家评审时应重点看系统是否把风险暴露出来，而不是要求所有异常都自动修复。

## 4. 证据链如何查看？

常见查看入口：

- 报告详情页。
- 勾稽关系和问题项。
- 比对详情页中的差异来源说明。
- GovInsight 打印页中的来源状态。

可见内容包括字段路径、原始值、解析值、比对值、风险原因、来源线索、GovInsight 的 `payloadSource`、`materializeStatus`、`sourceJobId`、`sourceReportVersionId`、`dataQuality` 摘要。

边界是：系统当前不承诺每一项都有页码、表格坐标或 OCR 原文。没有这些来源时，应显示保守口径。

## 5. PDF 报告是否可追溯？

PDF 导出通过任务化链路生成，推荐主路径是 `/api/pdf-jobs` 和 React 打印页。任务中心可查看 PDF 导出任务状态、完成时间、失败原因、文件是否过期和重新生成入口。

可追溯内容包括：

- comparison id。
- PDF job id。
- 任务状态。
- 导出标题和文件名。
- 创建、开始、完成时间。
- 文件是否存在。

当前 PDF smoke 可检查 comparison PDF、长表格 PDF、GovInsight PDF 和 PDF job API。若本机缺少 Poppler、ImageMagick、Ghostscript，像素级视觉回归会降级为文本、页数、空白页和 ready marker 检查。

## 6. 年报样本异常如何处理？

异常样本不应被静默当成正常样本。处理原则：

- 缺表、表头不足、跨页、长表格截断等进入风险提示或复核。
- 存在 open issue 的报告应阻断发布或至少明确提示。
- PDF 未完成或已过期时，不返回错误文件。
- GovInsight latest job 失败时，应说明已知边界，使用已存报告和 PDF 演示。

P4 推荐用 report `4839` 展示异常样本和发布阻断。

## 7. 如何保证不误判？

系统通过以下方式降低误判风险：

- 先报告入库、解析、复核、发布，再进入比对。
- 勾稽检查对异常项给出风险提示。
- `NOT_ASSESSABLE` 不直接改写成失败或通过。
- 证据说明不伪造缺失来源。
- 比对结果作为辅助材料，最终由人工确认。
- 权限和数据范围控制减少越权查看和误操作。

系统不能保证所有样本自动零误判。验收时应把“可见、可复核、可追踪、可阻断”作为核心标准。

## 8. GovInsight 指标来源如何说明？

GovInsight 指标来源应按当前系统显示的来源状态说明：

- 已存 payload。
- materialize 状态。
- source job id。
- source report version id。
- dataQuality 摘要。

对于推荐演示样本 `city_721` / `721`, year `2025`，P4-1/P4-2 已确认已存报告和 PDF 可用。latest job `17` failed 是已知遗留问题，不应被描述为已修复；现场不建议演示重新生成。

GovInsight 输出用于辅助治理分析，不替代正式业务结论或人工定责。

## 9. TaskDrawer 和任务中心的作用是什么？

TaskDrawer 是业务页面内的任务观察入口，便于用户在上传、解析、PDF 导出等流程中看到任务状态。

任务中心是集中管理入口：

- `/jobs` 查看上传解析任务。
- `/jobs?tab=download` 查看 PDF 导出和下载任务。

任务中心可展示排队、处理中、成功、失败、过期、重新生成、批量下载等状态。TaskDrawer 的人工可视化确认建议按 `OPERATIONS.md` 使用真实登录账号补做。

## 10. 数据安全如何保障？

当前 P4 可说明的安全边界包括：

- 业务接口需要登录 token。
- 未登录访问受保护 API 返回 401。
- 权限不足返回 403 或不可见。
- 用户和地区管理入口按权限键控制。
- 数据范围按用户配置的地区及下级地区约束。
- 诊断包默认不收集真实 `.env` 值、secret、上传原文、PDF、ZIP、截图、HTML dump、完整日志或备份包。
- 文档明确禁止提交真实 `.env`、secret、token。

边界说明：P4-4 不做新的安全加固、渗透测试或权限模型改造；这些如需开展，应进入后续阶段。

## 11. 权限如何控制？

权限由三部分组成：

- 登录 token：确认用户身份。
- 权限键：如 `upload_reports`、`view_reports`、`manage_jobs`、`manage_regions`、`manage_users`、`delete_reports`、`system_admin`。
- 数据范围：`dataScope.regions` 指定可访问地区，系统递归包含下级地区。

前端导航会根据权限显示管理入口，后端接口也应按权限和数据范围过滤。

P4-1 已记录权限边界样本：admin、`huaian`、未登录，其中 `huaian` 可访问淮安市范围，不可访问宿豫区范围。

## 12. 如何备份恢复？

备份和恢复按 `OPERATIONS.md` 与 `DEPLOYMENT.md` 执行。

基本原则：

- `data/uploads` 必须纳入备份。
- 数据库和上传文件应匹配恢复。
- `data/exports/pdf` 可按保留期清理，过期 PDF 可重新生成。
- 恢复是高风险操作，应人工确认目标环境、备份来源和停机窗口。
- 旧恢复脚本如包含硬编码路径或默认凭据，不建议直接运行，应优先按备份包 `RESTORE.md` 和手工流程恢复。

P4-4 不新增或修改备份恢复脚本。

## 13. 如何回滚？

代码回滚按 `DEPLOYMENT.md` 和 `OPERATIONS.md` 的回滚流程执行。建议步骤包括：

1. 确认需要回滚的版本。
2. 停止相关服务。
3. 切回目标版本或部署包。
4. 重新安装依赖和构建。
5. 启动后端和前端。
6. 执行 `ops:health`、`smoke:pdf` 和关键页面抽查。

数据库回滚必须使用发布前备份，不能把 schema 或数据修复作为 P4-4 演示动作。

## 14. 如何扩展地区和年份？

地区和年份扩展的基本思路：

- 先在地区管理中维护地区层级。
- 上传对应地区和年份的年度报告。
- 等待解析任务完成。
- 完成人工复核和发布。
- 发布后进入比对和 GovInsight 相关流程。

注意事项：

- 新地区和新年份不是只加一个名称，还需要真实年报样本、解析复核和数据质量确认。
- 如果涉及 GovInsight 指标，需确认数据是否已物化或可生成。
- 扩展过程不应绕过权限和复核流程。

## 15. 与传统人工比对相比的价值是什么？

系统价值主要体现在：

- 把 PDF 年报转为可检索、可复核、可比对的数据。
- 把差异从人工逐页查找变成按字段、章节、表格定位。
- 把异常和不确定项显式暴露，减少漏看。
- 把 PDF 导出、任务状态和过期处理纳入可追踪流程。
- 把 GovInsight 分析和来源状态用于领导汇报和专家复核。
- 把部署、巡检、诊断、备份恢复和故障排查材料固化，便于交付。

系统不是替代专家判断，而是把重复劳动和证据整理工作系统化。

## 16. 系统边界和不承诺事项

本阶段不承诺：

- 所有年报样本都能自动完美解析。
- 所有异常都能自动修复。
- 系统检查可替代人工最终审核。
- 每个字段都有页码、表格坐标或 OCR 原文证据。
- GovInsight latest job 重新生成已修复。
- PDF 已完成像素级视觉回归，除非验收环境安装并验证了原生工具链。
- 已完成性能压测、安全渗透测试或 P4-5 加固。
- 已改造数据库 schema、解析算法、比对算法、GovInsight 生成逻辑或 PDF 主链路。

本阶段可以承诺：

- 已形成客户演示、验收标准、用户手册草稿和专家问答材料。
- 材料基于 P4-1/P4-2/P4-3 已有证据。
- 验收口径明确区分已完成能力、降级能力和遗留问题。
