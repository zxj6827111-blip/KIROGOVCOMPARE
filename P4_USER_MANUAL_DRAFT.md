# P4 用户手册草稿

## 1. 适用对象

本文档面向业务用户、客户管理员、项目交付人员和后续维护成员，说明 KIROGOVCOMPARE 当前主线功能的基础使用方式。

本文档是 P4 阶段用户手册草稿，只描述当前已有能力和操作口径，不代表新增功能承诺。

## 2. 使用前准备

使用前请确认：

- 已获得系统访问地址。
- 已获得账号和初始密码，或由管理员创建账号。
- 当前账号具备对应权限和数据范围。
- 如需上传或下载文件，浏览器应允许文件选择和下载。
- 如需导出 PDF，后端 PDF 导出服务应正常。

推荐浏览器：Chrome、Edge 或同等现代浏览器。

## 3. 登录

操作步骤：

1. 打开系统前端地址。
2. 在登录页输入用户名和密码。
3. 登录成功后进入年报工作台。
4. 如登录失败，检查账号密码、账号是否停用、后端服务是否可用。

注意事项：

- 未登录用户不能访问受保护业务数据。
- 登录状态过期后需要重新登录。
- 不要把账号、密码、token 写入截图、文档或聊天工具。

## 4. 年报维护

年报维护包括查看年报、检查年报状态、进入报告详情和处理问题。

常用入口：

- 年报工作台：`/catalog`
- 报告详情：`/catalog/reports/:reportId`
- 问题复核：`/issues/*`
- 年报维护：`/report-maintenance`

基本操作：

1. 进入年报工作台。
2. 按地区、年份或单位定位报告。
3. 点击报告进入详情。
4. 查看报告基础信息、版本状态、结构化数据和勾稽检查。
5. 如存在问题项，进入问题复核或报告详情中的定位入口。

注意事项：

- 报告进入比对前应先完成必要复核和发布。
- 存在 open issue 的报告不应直接作为正常样本演示。
- 异常样本应保留用于说明系统如何阻断和提示风险。

## 5. 上传

常用入口：`/upload`

操作步骤：

1. 点击“上传报告”。
2. 选择地区、年份和单位。
3. 选择年度报告 PDF 文件。
4. 提交上传。
5. 系统创建解析任务，页面或 TaskDrawer 会提示任务状态。
6. 可进入任务中心查看详细进度。

注意事项：

- 上传文件应为目标年度政府信息公开年报。
- 不建议现场批量上传未知样本作为正式验收依据。
- 如系统复用已存在报告版本，应以页面和任务中心显示为准。
- 上传失败时，先查看错误提示和任务中心，不要重复上传大量文件。

## 6. 解析任务

常用入口：`/jobs`

任务状态一般包括：

- 排队中：任务已创建，等待处理。
- 处理中：系统正在解析或生成。
- 成功：任务完成。
- 失败：任务执行失败，需要查看原因。
- 已取消：任务被取消。

操作步骤：

1. 进入任务中心。
2. 选择上传解析任务 tab。
3. 按地区、年份、单位或状态筛选。
4. 查看任务进度、步骤名称、失败原因和创建时间。
5. 成功任务可进入报告详情继续复核。

异常处理：

- 任务长时间排队：联系运维查看任务队列和服务状态。
- 任务失败：记录 job id、报告 id、错误信息，按 `TROUBLESHOOTING.md` 排查。
- 任务被取消：确认是否为用户主动取消或管理员处理。

## 7. 复核

复核目标是确认系统解析结果、勾稽检查和异常提示是否符合业务事实。

操作步骤：

1. 打开报告详情。
2. 查看结构化数据表。
3. 查看勾稽检查结果。
4. 对 `FAIL`、`UNCERTAIN`、`NOT_ASSESSABLE` 项查看证据说明。
5. 点击定位入口，回到相关字段或表格位置。
6. 根据业务资料和原始年报进行人工确认。

复核口径：

- 系统检查用于提示风险和减少人工查找成本。
- 最终确认仍需人工复核。
- 当系统只显示字段路径、风险原因或来源线索时，不应理解为已有完整页码、表格坐标或 OCR 原文证据。
- 不可评估项不等于失败，也不等于通过，应结合人工判断。

## 8. 发布

发布表示某份报告版本可作为后续比对和展示的有效版本。

操作步骤：

1. 完成必要复核。
2. 确认没有阻断发布的 open issue。
3. 由具备权限的用户执行发布操作。
4. 发布后报告可进入比对链路。

注意事项：

- 存在未处理阻断问题时，系统应拒绝发布或提示风险。
- 如 report `4839` 这类异常样本存在 open issue，应作为发布阻断演示样本。
- 不要通过数据库或脚本绕过发布流程。

## 9. 比对

常用入口：

- 比对中心：`/history`
- 比对详情：`/comparison/:comparisonId`

操作步骤：

1. 进入比对中心。
2. 按地区、年份或状态查看历史比对。
3. 打开目标比对详情。
4. 查看差异摘要、相似度、检查状态、章节差异和表格差异。
5. 查看差异来源说明。
6. 如需归档，发起 PDF 导出任务。

推荐样本：

- comparison `4670`：淮安市 2024-2025。
- comparison `1143`：清江浦区工业和信息化局 2024-2025。

注意事项：

- 当前主线是 report-based compare：先报告入库、复核、发布，再比对。
- 旧版 `/api/v1/tasks/compare/*` 不作为业务可用入口。
- 比对结果是辅助复核材料，不替代人工最终判断。

## 10. 证据链查看

证据链入口主要在报告详情、勾稽检查、比对详情和 GovInsight 打印页。

可查看内容包括：

- 字段路径。
- 原始值、解析值、比对值。
- 风险原因。
- 来源线索。
- GovInsight 来源状态，例如已保存 payload、source job、source report version、dataQuality 摘要。

使用建议：

- 先看检查结论，再看证据说明。
- 对无法确认的项，回到原始年报或业务材料人工核对。
- 不要把“暂无更详细来源”理解为系统已提供完整证据。
- 对复杂表格、跨页、缺表、表头不足等场景，应保守复核。

## 11. PDF 导出

常用入口：

- 比对详情页中的 PDF 导出入口。
- 任务中心下载 tab：`/jobs?tab=download`。

操作步骤：

1. 在比对详情中发起 PDF 导出。
2. 系统创建 PDF 导出任务。
3. 打开任务中心下载 tab。
4. 查看任务状态：可下载、生成中、失败、已过期。
5. 对可下载任务点击下载。
6. 对已过期任务点击重新生成。

注意事项：

- PDF 导出是任务化流程，生成完成后才能下载。
- 文件过期时需要重新生成。
- 失败任务不会提供错误 PDF 文件。
- 当前 PDF smoke 在缺少原生视觉工具时会降级为文本、页数、空白页和 ready marker 检查。

## 12. 任务中心

常用入口：`/jobs`

任务中心包含两个核心区域：

- 上传解析任务：查看报告解析任务。
- 下载任务：查看 PDF 导出任务，可通过 `?tab=download` 直接进入。

上传解析任务可执行：

- 查看状态。
- 查看详情。
- 取消进行中任务。
- 删除任务记录。
- 按条件筛选。

下载任务可执行：

- 查看 PDF 导出状态。
- 下载已完成且未过期文件。
- 重新生成过期文件。
- 删除下载任务。
- 批量下载已完成且未过期文件。

注意事项：

- 批量下载只会打包可下载文件。
- 过期文件应先重新生成。
- 删除任务或文件前应确认是否仍需留存。

## 13. 批量下载

系统当前支持 PDF 下载任务的批量打包下载。

使用条件：

- 进入 `/jobs?tab=download`。
- 勾选一个或多个 PDF 下载任务。
- 被打包的任务必须是“已完成且文件未过期”。

操作步骤：

1. 进入任务中心下载 tab。
2. 勾选需要下载的任务。
3. 点击批量下载。
4. 浏览器下载 ZIP 文件。

限制说明：

- 未完成任务不会被打包。
- 失败任务不会被打包。
- 已过期文件不会被打包，需要先重新生成。
- 批量下载行为受用户数据范围权限约束。

## 14. GovInsight

常用入口：

- 智能治理模块：`/govinsight/*`
- GovInsight 打印页：`/print/govinsight-report/:orgId/:year`
- GovInsight PDF API：`/api/gov-insight/report-pdf?org_id=...&year=...`

操作步骤：

1. 进入智能治理模块。
2. 选择地区和年份。
3. 查看年度数据、指标分析和报告内容。
4. 查看来源状态和数据质量摘要。
5. 如需归档，打开打印页或导出 PDF。

当前推荐演示样本：

- `city_721`，year `2025`。
- `721`，year `2025`，用于说明 numeric orgId 兼容。

注意事项：

- 当前演示建议使用已存报告和 PDF。
- P4-1 已知 GovInsight latest job `17` failed，但历史成功 job `16`、已存报告和 PDF 可用。
- 不建议现场演示 latest job 重新生成。
- GovInsight 指标用于辅助分析，不替代正式业务定责。

## 15. 权限和角色说明

系统通过登录 token、权限键和数据范围控制访问。

常见权限口径：

- `upload_reports`：上传年报。
- `view_reports`：查看报告。
- `manage_jobs`：管理任务。
- `manage_regions`：管理地区。
- `manage_users`：管理用户。
- `delete_reports`：删除报告。
- `system_admin`：系统管理员相关权限。

数据范围：

- 用户可被配置到指定地区范围。
- 系统按地区及下级地区计算可访问数据。
- 权限外数据应不可见或被接口拒绝。

注意事项：

- 管理入口根据权限显示。
- 普通用户不应使用管理员账号完成日常验收。
- 权限问题应先确认账号配置，不直接判定为系统故障。

## 16. 常见问题

### 登录后页面仍回到登录页

可能原因：

- token 过期。
- 后端 API 不可用。
- 浏览器缓存或本地登录状态异常。

处理建议：

- 重新登录。
- 刷新页面。
- 联系运维检查 `/api/health`。

### 上传后没有看到解析结果

可能原因：

- 任务仍在排队。
- 任务失败。
- 上传样本格式或内容异常。

处理建议：

- 打开 `/jobs` 查看任务状态。
- 记录 job id 和错误信息。
- 按 `TROUBLESHOOTING.md` 排查上传解析失败。

### 报告无法发布

可能原因：

- 存在 open issue。
- 数据质量风险未处理。
- 当前用户没有权限。

处理建议：

- 查看报告详情和问题项。
- 完成复核后再发布。
- 联系管理员检查权限。

### PDF 下载提示过期

说明 PDF 文件已被清理或超出保留期。

处理建议：

- 在下载任务中点击重新生成。
- 等任务完成后再次下载。

### 批量下载按钮不可用

可能原因：

- 未勾选任务。
- 勾选的任务没有可下载文件。
- 文件已过期。

处理建议：

- 只选择已完成且未过期任务。
- 过期任务先重新生成。

### GovInsight 最新任务失败怎么办

如果使用 P4 推荐样本，当前已存报告和 PDF 仍可用。latest job `17` failed 是已知遗留问题，不建议现场重新生成。需要修复时应进入后续问题处理阶段。

## 17. 注意事项

- 不要把系统检查结果当作最终人工结论。
- 不要把不可评估项直接视为通过。
- 不要在客户现场临时运行数据库迁移、恢复、清理或删除操作。
- 不要提交或传播真实 `.env`、secret、token。
- 不要把 PDF、ZIP、截图、HTML dump、诊断包、日志作为 Git 提交内容。
- 不要把 P4-5 性能、安全、权限加固写成本阶段已完成。

## 18. 异常处理建议

| 异常 | 建议处理 |
| --- | --- |
| 后端不可用 | 运行或联系运维运行 `npm.cmd run ops:health` |
| 前端不可用 | 检查前端服务地址和 `/healthz` |
| 上传失败 | 查看任务中心错误，记录样本和 job id |
| 解析失败 | 查看失败原因，判断样本问题还是服务问题 |
| PDF 失败 | 查看 `/jobs?tab=download`，必要时重新生成 |
| PDF 过期 | 重新生成后下载 |
| 权限不足 | 检查账号权限和数据范围 |
| GovInsight 报告异常 | 区分已存报告、payload、latest job 和 PDF 路径 |
| TaskDrawer 不刷新 | 刷新任务中心，对照接口和运维手册排查 |

## 19. 运维材料引用

更多部署和运维内容请参考：

- `DEPLOYMENT.md`：部署、端口、环境变量、构建、PDF、smoke、回滚。
- `OPERATIONS.md`：日常巡检、任务队列、目录、备份、恢复、TaskDrawer 人工验收。
- `TROUBLESHOOTING.md`：登录、权限、上传、任务、PDF、GovInsight、诊断和生成物误入 Git 排查。
- `P4_DEPLOYMENT_OPERATIONS_REPORT.md`：P4-3 运维交付能力实施和验证证据。
