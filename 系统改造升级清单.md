# 系统改造升级清单

## 1. 文档目的

本清单用于指导当前系统的技术升级与工程改造，目标不是“全面重写”，而是在保证现有业务可持续上线的前提下，优先解决已经影响稳定性、部署一致性和后续扩展效率的问题。

当前判断：

- 系统核心业务链路可用，不建议推倒重来。
- 需要优先升级的是工程底座，而不是业务功能本身。
- 本轮改造应遵循“先稳住、再统一、后优化”的原则。

---

## 2. 总体结论

### 2.1 当前最需要升级的部分

1. 前端构建链路老化，仍基于 `react-scripts 5.0.1 / CRA`。
2. 本地、CI、容器三套 Node 运行环境不一致。
3. Docker/部署配置与当前实际服务入口已有偏差。
4. 鉴权方案偏自研，安全性与标准化程度不足。
5. 后台任务体系分裂，存在 Redis 队列与轮询 Worker 并存的情况。

### 2.2 暂时不需要立即重构的部分

1. 后端主框架 `Express + TypeScript + PostgreSQL` 仍可继续使用。
2. React 18、Tailwind 3、Recharts 3、Puppeteer 24、OpenAI SDK 6 暂不构成当前主要瓶颈。
3. 当前业务页面与报告能力应以稳定上线为第一目标，不建议现在就做大规模界面重写。

---

## 3. 改造优先级总表

| 优先级 | 改造项 | 当前问题 | 建议动作 | 目标结果 |
| --- | --- | --- | --- | --- |
| P0 | 运行环境统一 | Node 版本不一致，行为不可预测 | 统一 Node 版本到 LTS，并同步 CI 与 Docker | 本地、测试、部署环境表现一致 |
| P0 | 前端构建链升级 | CRA 老化，Windows/Node 新版本兼容性差 | 从 CRA 逐步迁移到 Vite | 启动更快，构建更稳，兼容性更好 |
| P0 | 部署配置修正 | Dockerfile 与当前主服务入口不一致 | 修正镜像入口、端口、健康检查和构建流程 | 可稳定部署当前 LLM/GovInsight 服务 |
| P1 | 鉴权体系升级 | 自定义 token + localStorage 风险较高，且存在已知安全缺陷 | 先修现有安全缺陷，再迁移到 httpOnly cookie，并评估标准 Session/JWT 方案 | 提高安全性与可维护性 |
| P1 | 任务系统统一 | 多套独立 Worker 并存，且有死代码残留 | 清理遗留路径，统一后台任务框架与状态模型 | 重试、取消、监控机制一致 |
| P1 | 前端类型体系收口 | JS/TS 混编，维护成本高 | 新代码统一 TS，旧代码分阶段迁移 | 降低回归风险与改动成本 |
| P2 | PDF 解析/导出链升级 | 解析链存在 legacy 与运行时兼容问题 | 优化 PDF 解析依赖与渲染环境 | 降低 PDF 相关异常率 |
| P2 | 依赖与历史残留清理 | 存在未完成能力与冗余依赖 | 清理无效依赖与历史脚本 | 提升项目整洁度 |
| P2 | 监控与告警增强 | 目前主要依赖日志排查 | 增加 job 监控、错误统计、健康检查面板 | 更快定位线上问题 |

---

## 4. 分阶段改造计划

## 第一阶段：稳定上线必做项（建议 1-2 周内完成）

### 4.1 统一 Node 运行环境

#### 当前问题

- 本地实际运行环境为 Node 24。
- CI 当前固定为 Node 20。
- Dockerfile 当前仍使用 Node 18。
- 这会导致“本地正常、CI 异常、部署表现不同步”的问题。

#### 改造建议

1. 统一目标版本为 `Node 20 LTS` 或 `Node 22 LTS`。
2. 在根目录补充 `.nvmrc` 或者明确版本说明。
3. 同步更新以下文件：
   - `package.json`
   - `.github/workflows/ci.yml`
   - `Dockerfile`
4. 本地开发、CI、生产部署全部按同一版本执行。

#### 验收标准

- 后端构建、前端构建、PDF 导出、任务 Worker 在三套环境表现一致。
- 不再出现因 Node 版本差异导致的前端启动异常。

### 4.2 前端构建链从 CRA 迁移到 Vite

#### 当前问题

- 当前前端仍使用 `react-scripts 5.0.1`。
- 已经出现 `spawn EPERM` 兼容性问题，需要 wrapper 绕过。
- CRA 已经不再是现代主流构建方案，后续维护成本会越来越高。

#### 改造建议

1. 新建 Vite 配置，先保持页面结构不变。
2. 迁移以下能力：
   - React 入口
   - 环境变量读取方式
   - 静态资源路径
   - 开发代理
3. 将现有 `react-scripts-wrapper.js` 视为临时兼容方案，迁移完成后删除。
4. 保持当前业务页面与路由结构尽量不动，避免业务风险外溢。

#### 预期收益

- 启动速度更快。
- 构建稳定性更高。
- 与新版本 Node 兼容性更好。
- 后续前端迭代成本更低。

### 4.3 修正 Docker 与部署入口

#### 当前问题

- 当前 Dockerfile 仍指向旧入口。
- 当前主服务已是 `index-llm` 链路，但部署文件未完全同步。
- 端口与健康检查路径也存在历史配置遗留。

#### 改造建议

1. 更新 Dockerfile，使其构建并运行当前实际主服务。
2. 将健康检查统一到当前有效接口。
3. 明确前后端部署模式：
   - 前后端分离部署
   - 或后端静态托管前端构建产物
4. 补齐生产环境所需环境变量模板。

#### 验收标准

- 一套容器配置即可拉起当前生产链路。
- 健康检查、日志路径、端口暴露与现状一致。

---

## 第二阶段：工程一致性改造（建议 2-4 周内完成）

### 4.4 鉴权体系升级

⚠️ 本节策略已按 §9.5 修正：优先修复现有安全缺陷，再决定是否引入标准 JWT 库；不建议把“替换认证方案”排在已知漏洞修复之前。

#### 当前问题

- 当前使用自定义 HMAC token。
- 前端将 `admin_token` 存放在 `localStorage`。
- 现阶段更紧迫的问题不是“是否标准”，而是明文日志、非恒时比较、以及前端持久化 token 带来的安全暴露面。

#### 改造建议

1. 第一优先级先修复现有安全缺陷：
   - 删除明文敏感日志
   - 将签名与密码校验改为恒时比较
   - 为接口补齐标准安全头
2. 第二优先级将登录态逐步迁移到 `httpOnly cookie`。
3. 在上述问题收口后，再评估采用标准 JWT 库或 session 方案，而不是先为“标准化”而重做。
4. 引入更清晰的 token 续期策略：
   - access token
   - refresh token
5. 补充登录态失效、退出登录、跨页恢复等标准处理逻辑。

#### 预期收益

- 安全性更高。
- 登录状态管理更规范。
- 减少前端手工处理 token 的成本。

### 4.5 统一后台任务体系

⚠️ 本节策略已按 §9.2.1 和 §9.5 修正：Bull Queue 已确认为死代码路径，当前不应再把它视为活跃任务体系的一部分。

#### 当前问题

- 当前活跃任务体系主要是：
  - `LlmJobRunner`
  - `GovInsightReportJobWorker`
  - `PdfExportWorker`
- 历史上的 Bull 队列代码仍然存在，但处理器未完成且未有效接入，属于应清理的遗留路径。
- 如果不先清理死代码，再做任务抽象统一，后续讨论会持续混淆“现役机制”和“遗留机制”。

#### 改造建议

1. 先删除或隔离 Bull 死代码，避免继续误导维护者。
2. 以现有 PostgreSQL Worker 体系为基础统一任务抽象：
   - 任务表
   - 任务状态
   - 重试机制
   - 超时机制
   - 取消机制
3. 统一 Worker 启动方式与日志输出格式。
4. 为 GovInsight 报告、PDF 导出、其他 AI 任务复用同一套任务框架。

#### 验收标准

- 所有后台任务都能统一查看状态。
- 失败、取消、超时、重试都有一致行为。

### 4.6 前端 JS/TS 混编收口

#### 当前问题

- 前端当前仍存在较多 `.js` 文件与 `.tsx` 并存。
- `allowJs: true` 虽然方便兼容，但不利于长期维护。

#### 改造建议

1. 新增页面和新组件一律使用 TypeScript。
2. 旧 JS 文件优先迁移以下模块：
   - 登录
   - 权限
   - 报告详情
   - 数据管理核心页面
3. 建立页面类型、接口类型、状态类型的统一定义目录。

#### 预期收益

- 提高改动安全性。
- 降低页面联调出错率。
- 便于后续大型页面改造。

---

## 第三阶段：质量与可维护性提升（建议 1-2 个月内完成）

### 4.7 PDF 解析与导出链优化

#### 当前问题

- PDF 解析仍有 legacy 依赖。
- 当前运行中已经出现 `canvas` 缺失告警。
- PDF 能力对当前系统是核心能力之一，不能长期依赖“带警告可运行”状态。

#### 改造建议

1. 梳理 PDF 解析与 PDF 导出的两条链路。
2. 明确以下组件的职责边界：
   - PDF 解析
   - 页面渲染
   - PDF 导出
3. 固定 Puppeteer 运行时依赖。
4. 清理 legacy 兼容层，减少运行期隐性错误。

### 4.8 清理历史依赖与未完成能力

#### 当前问题

- 存在未真正落地的能力，例如 S3 存储接口仍是 TODO。
- 部分依赖可能是历史残留，并未形成稳定能力闭环。

#### 改造建议

1. 清理未使用或半成品依赖。
2. 对保留但未实现完成的模块做二选一处理：
   - 正式补完
   - 彻底移除
3. 对历史脚本与遗留文档做归档。

### 4.9 补强监控与告警

#### 当前问题

- 当前主要依赖日志定位问题。
- 对任务超时、失败率、外部模型异常缺少统一监控面板。

#### 改造建议

1. 增加关键任务指标：
   - 任务数
   - 失败率
   - 平均耗时
   - 外部模型异常率
2. 增加错误码维度的聚合统计。
3. 为长耗时任务增加明确的超时与告警机制。

---

## 5. 推荐实施顺序

### 第一批，马上动

1. 统一 Node 版本。
2. 修正 Docker/部署入口。
3. 保留现有业务功能不变，完成前端构建链迁移到 Vite。

### 第二批，系统稳定后继续

1. 鉴权标准化。
2. 统一后台任务框架。
3. 前端核心页面逐步 TypeScript 化。

### 第三批，作为平台化优化

1. PDF 解析/导出能力治理。
2. 历史依赖与旧脚本清理。
3. 监控、告警、运行观测完善。

---

## 6. 本项目的改造原则

1. 不做一次性大重构，避免业务风险集中爆发。
2. 优先处理已经影响稳定性的工程问题。
3. 所有升级都应以“业务可持续交付”为前提。
4. 升级前后要保留可回滚路径。

---

## 7. 建议的下一步行动

建议按照以下顺序进入实施：

1. 先出一份“P0 升级实施方案”，限定范围只做：
   - Node 版本统一
   - Docker/部署修正
   - CRA 到 Vite 迁移
2. 实施完成后，再进入第二阶段。
3. 鉴权升级和任务系统统一建议单独成项目执行，不与当前上线任务混做。

---

## 8. 当前建议结论

当前系统适合“分阶段升级”，不适合“推倒重做”。

如果只从投入产出比看，最值得先做的三件事是：

1. 统一运行环境。
2. 前端从 CRA 迁移到 Vite。
3. 修正部署入口并补齐前端构建纳入 CI。

这三项完成后，系统的稳定性、维护性和后续扩展空间都会明显改善。

---

## 9. 2026-04-11 复核补充

基于对当前代码的二次核查，原始清单对安全与架构层面的风险评估偏轻，需要补充以下结论。

### 9.1 应前置到 P0 的问题

1. 登录接口存在明文敏感信息落日志问题。
   - 位置：`src/routes/auth.ts:14`
   - 现状：`console.log('[Auth] Login attempt:', { username, body: req.body });` 会把密码写入日志。
2. 鉴权链路存在非恒时比较。
   - 位置：`src/middleware/auth.ts:79`、`src/middleware/auth.ts:120`
   - 现状：token 签名与密码哈希都使用普通字符串比较，存在 timing attack 风险。
3. 安全头缺失。
   - 位置：`src/app-llm.ts`
   - 现状：当前只有手写 CORS，没有 `helmet`，缺少 `X-Content-Type-Options`、`X-Frame-Options`、CSP 等标准安全头。
4. 数据库连接池配置缺失且存在双池并存。
   - 位置：`src/config/database.ts`、`src/config/database-llm.ts`
   - 现状：两个 `pg.Pool` 都使用默认参数；按文件数统计，仍有 11 个文件使用旧连接池，若按引用点统计则为 13 处。
5. GovInsight 报告读写接口当前仍处于公开暴露状态。
   - 位置：`src/routes/gov-insight.ts:52-54`、`src/routes/gov-insight.ts:648`、`src/routes/gov-insight.ts:693`
   - 现状：GovInsight 路由层默认未全局启用鉴权；其中 `/ai-report/jobs` 系列端点已单独加了 `authMiddleware`，但 `/ai-report/save` 与 `/ai-report` 仍可直接写入和读取报告数据。
   - 影响：如果服务暴露到公网，存在未授权读取与篡改辅策报告内容的风险。
   - 修复复杂度：很低，优先直接为这两个端点补上 `authMiddleware`。

### 9.2 架构问题比原清单更严重

1. Bull Queue 路径基本属于死代码，应先清理再谈重构。
   - 位置：`src/queue/processors.ts`
   - 现状：compare processor 明确返回 `COMPARE_DISABLED`，其余 processor 仍是 `TODO`，`setupAllProcessors()` 无有效接入。
   - 建议：先删除死代码，统一到现有 PostgreSQL `SELECT ... FOR UPDATE SKIP LOCKED` Worker 体系，再抽共享 `BaseJobWorker`。
2. 数据库迁移机制已成为高风险点。
   - 位置：`src/db/migrations-llm.ts`
   - 现状：按当前工作区快照统计，文件约 845 行，包含大量 schema 变更与数据修复逻辑，缺少标准版本化迁移追踪。
   - 建议：迁移到 `node-pg-migrate` 一类增量迁移方案。
3. 前端主路由仍是手写路由壳。
   - 位置：`frontend/src/App.js`
   - 现状：使用 `window.history.pushState`、`popstate`、`pathname.split('/')` 手动解析。
   - 影响：不利于代码分割、404 处理、路由保护与可维护性。
4. 路由层业务过重。
   - 位置：`src/routes/reports.ts`
   - 现状：约 1578 行、64.8KB，路由层直接承载大量 SQL 与业务逻辑。

### 9.3 需要纠偏的表述

1. “前端没有使用 react-router-dom”不准确。
   - 现状：主壳 `frontend/src/App.js` 没有使用，但 GovInsight 子模块已使用 `react-router-dom`。
   - 位置：`frontend/src/govinsight/DashboardApp.tsx` 等 4 处。
2. “前端 0 测试”不准确，但整体测试能力仍明显不足。
   - 现状：后端测试 2 个，前端测试 1 个；按当前工作区快照统计，`src/services` 下服务文件 47 个。
   - 结论：应表述为“测试覆盖极低”，而不是“完全没有测试”。
3. “Redis 在 memory 模式下也会建立连接”需要更谨慎表述。
   - 现状：`src/config/redis.ts` 会预创建 client 对象，但当前限流 Redis 连接由 `src/middleware/rateLimit.ts` 惰性建立。
   - 结论：更准确的说法是“存在疑似遗留 Redis 客户端，应审计并清理”，而不是“当前一定会多余连接 Redis”。
4. “Print 路由完全绕过鉴权”需要区分两个场景。
   - 对比报告导出链路中，`/print/comparison/*` 页面本身不做登录页拦截，但实际数据请求仍依赖短时 `service_token` 或已有登录 token。
   - 更高风险的不是这个页面壳本身，而是 GovInsight 公共 API 当前默认开放，导致打印页与普通页面都可直接读取相关数据。

### 9.3.1 建议补充的改造点

1. 前端类型依赖版本与实际运行时存在明显错配。
   - 位置：`frontend/package.json`
   - 现状：`react`/`react-dom` 为 18，但 `@types/react`、`@types/react-dom` 已升到 19；`react-scripts 5.0.1` 仍绑定较旧测试栈，而前端 `@types/jest` 已升到 30，`@types/node` 也显著超前。
   - 影响：会放大类型噪音、降低类型检查可信度，并增加 CRA 到 Vite 迁移时的兼容性成本。

### 9.4 更新后的实施顺序建议

1. 第一优先级先修安全缺陷。
   - 删除登录明文日志
   - 改为恒时比较
   - 增加安全头
2. 第二优先级收口基础设施分裂。
   - 清理 Bull 死代码
   - 合并双数据库连接池
   - 给连接池补充 `max`、`idleTimeoutMillis`、`connectionTimeoutMillis`
3. 第三优先级治理长期演进成本。
   - 引入标准迁移系统
   - 拆分超大路由
   - 让主前端路由切到 `react-router-dom`
   - 收敛前端 `@types` 与运行时版本

### 9.5 对原改造策略的修正

1. 不建议为了“看起来现代化”立即引入 BullMQ。
   - 现有 PostgreSQL Worker 机制已可支撑当前场景，优先清理死路径、统一抽象更务实。
2. 鉴权暂不必为了标准化而立即接入 JWT 第三方库。
   - 更重要的是先修安全缺陷，再逐步迁移到 `httpOnly cookie` 模式。
3. CRA 到 Vite 迁移仍值得做，但不应排在明显安全漏洞之前。
4. 若继续把 GovInsight 保持为“部分公开数据、部分内部报告”的混合形态，必须先把公开接口与内部接口拆层，而不是继续共用同一个无鉴权路由文件。

截至本次复核，累计可识别的改造点已由原始清单的 9 项扩展到 27 项，且其中一部分需要上调优先级。
