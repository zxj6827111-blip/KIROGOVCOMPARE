# GovInsight 底座方案冻结补充单

文档日期：2026-04-14  
用途：实施前最终确认  
适用前提：本补充单建立在 [GOVINSIGHT_UNIT_TYPE_PARENT_YEAR_FOUNDATION_PLAN_2026-04-14.md](./GOVINSIGHT_UNIT_TYPE_PARENT_YEAR_FOUNDATION_PLAN_2026-04-14.md) 已评审通过的大方向之上

---

## 一、任务结论摘要

### 1.1 结论

当前底座方案已经足够接近“冻结后开工”状态。  
缺的不是新的调研，也不是新的方向，而是把少数会直接影响第一阶段实施的决定项正式拍板。

本补充单给出的最终判断是：

1. 现在可以把 `unit_type / parent_region_id / year` 底座视为第一阶段实施基础。
2. 但必须再冻结 5 类决定项，否则实施中一定返工：
   - `canonical_units` 的最小结构边界
   - `gov_open_annual_stats_v2` 的物理形态
   - official / preview / blocked 决策表
   - `unknown` 的治理边界
   - 首批人工映射范围
3. 这 5 类事项一旦确认，不需要再做新一轮摸底，可以直接进入第一阶段实施任务清单编制。

### 1.2 本补充单的拍板结论

本补充单建议直接冻结以下结论：

1. `canonical_units` 第一阶段就落表，且必须包含 `city_region_id`。
2. `unit_type` 正式枚举本轮即冻结，不再继续讨论扩类。
3. `functional_zone / district / department / town_street` 的大边界已经足够清晰，可以直接实施；但必须补一份“判定优先级 + 特殊白名单”作为冻结附件。
4. `parent_region_id` 正式来源为 `regions.parent_id`，最终以 `canonical_units` 固化结果为准。
5. `gov_open_annual_stats_v2` 主方案应采用“表”，不采用“物化视图”作为第一阶段主实现。
6. official 只允许 published 版本进入。
7. `unknown` 不允许进入 official，也不允许进入任何用户可见 preview，只能进入内部治理清单或 blocked 结果。
8. 第一阶段不需要全量人工过 1909 个实体，只需要覆盖“低置信 + 高风险边界模式 + 直辖市特殊白名单”。

### 1.3 对开工判断的明确回答

可以进入“第一阶段实施任务清单”编制阶段。  
前提是负责人明确签字确认本补充单中的冻结项，不再把这些事项留给开发中途再定。

---

## 二、必须冻结的事项总表

| 冻结事项 | 当前建议 | 是否建议现在冻结 | 不冻结的风险 | 是否影响第一阶段开工 |
| --- | --- | --- | --- | --- |
| `canonical_units` 粒度与结构边界 | 第一阶段按“每个 `region_id` 一条 canonical 主记录”实施，保留时间有效字段但先不启用多版本行 | 是 | 后续表结构、唯一键、API 和补跑逻辑一起返工 | 是 |
| `unit_type` 正式枚举 | 冻结为 `province/city/district/department/town_street/functional_zone/unknown` | 是 | 后续 rules、stats_v2、payload、驾驶舱全部返工 | 是 |
| 类型判定优先级 | 冻结“人工映射 > 直辖市特殊白名单 > town_street > functional_zone > department > district > unknown” | 是 | 同名规则在不同模块实现不同，导致口径漂移 | 是 |
| `functional_zone / district` 边界 | `新区` 不单独成类；由白名单和父级语义决定是 `district` 还是 `functional_zone` | 是 | `浦东新区`、`湖滨新区` 这类样本反复误判 | 是 |
| `department / functional_zone` 边界 | 名称含 `管委会` 不自动等于 `department`，先判断是否为功能区本体 | 是 | 开发区管委会、商务区管委会混判 | 是 |
| `town_street` 边界 | `镇/乡/街道/街道办事处` 优先判 `town_street` | 是 | 街道办被错归部门，区县比较失真 | 是 |
| `parent_region_id` 正式来源 | 默认取 `regions.parent_id`，以 `canonical_units.parent_region_id` 为最终结果 | 是 | 每个模块继续自己递归推断父级 | 是 |
| `city_region_id` 是否纳入底座 | 必须纳入 `canonical_units`，也必须进入 `stats_v2` | 是 | 主报告、区县表、部门表、驾驶舱都要重复写归属地市逻辑 | 是 |
| `year` official 口径 | 唯一来源为 `reports.year`，`year IS NULL` 永不进入正式消费 | 是 | 正式与预览、空壳与真实数据继续混用 | 是 |
| official / preview / blocked 边界 | 冻结固定决策表，禁止自由发挥 | 是 | 实施期最容易口径漂移，且返工成本最高 | 是 |
| `gov_open_annual_stats_v2` 形态 | 第一阶段采用“表”，不采用“物化视图”主实现 | 是 | 审计、回放、补跑、preview 管理都会受限 | 是 |
| `unknown` 治理规则 | 不进 official，不进用户可见 preview，只进 blocked 或内部治理清单 | 是 | 不明单位混入报告、监测表和驾驶舱 | 是 |
| 首批人工映射范围 | 不全量手工；先覆盖低置信、边界模式命中、有报告高风险实体和直辖市特殊白名单 | 是 | 自动识别边界样本高概率误判 | 是 |
| 新旧口径对账方式 | 冻结“三层对账”：raw 指标、类型拆分、强制样本抽检 | 是 | 上线前无法判断差异是 bug 还是口径变化 | 是 |
| 历史组织变更的真实治理流程 | 第一阶段先预留字段，不启用多时态映射 | 否 | 若现在强做会拖慢落地；若不预留字段则未来返工 | 否 |
| 驾驶舱 UI 联动改造 | 第二阶段处理，第一阶段只先提供稳定底座字段 | 否 | 不影响底座落地，但影响前端切换时机 | 否 |
| 区县/部门直接进入主报告正文 | 第二阶段再决定进入正文还是附件/画像 | 否 | 不影响底座与规则层先落 | 否 |

---

## 三、关键问题最终建议

### 3.1 `canonical_units` 是否从第一天就支持时间有效

#### 最终建议

建议：

1. 第一阶段就预留 `effective_from_year / effective_to_year` 字段。
2. 第一阶段不要求填充历史有效期。
3. 第一阶段的实际运行规则仍然是“一个 `region_id` 一条主映射记录”。

#### 明确回答

| 问题 | 最终建议 |
| --- | --- |
| 是否建议保留 `effective_from_year / effective_to_year` | 是 |
| 第一阶段是否必须填充 | 否 |
| 如果暂不启用，是否仍建议预留字段 | 是 |
| 如果不预留，未来风险是什么 | 历史组织变更一旦启用，需要改表、改唯一键、改接口、改补跑逻辑，返工面很大 |

#### 拍板理由

1. 现在预留字段的成本极低。
2. 未来一旦出现按年份调整的单位挂载关系，不预留会直接牵动 `canonical_units`、`stats_v2`、唯一键和导数接口。
3. 但第一阶段若直接启用多时态映射，会让问题复杂度明显上升，不值得。

#### 冻结表述

建议冻结为：

1. `canonical_units` 结构预留时间有效字段。
2. 第一阶段所有记录默认 `effective_from_year = NULL`、`effective_to_year = NULL`。
3. 第一阶段禁止同一 `region_id` 出现多条并存有效映射。

### 3.2 `gov_open_annual_stats_v2` 到底采用“表”还是“物化视图”

#### 最终建议

主方案明确选择：**表**。

#### 为什么选“表”

1. `official / preview / blocked_*` 是运行状态，不是单纯聚合结果，更适合表。
2. `source_report_version_id / mapping_version / metric_version / stats_snapshot_at` 都是审计字段，表更自然。
3. 第一阶段需要支持按批补跑、按年份补跑、按单单位回放，这些都更适合表。
4. 未来若要并存 official 与 preview，同一个 `region_id + year` 可能有多种物化状态，表更稳。
5. 物化视图更适合“确定性纯聚合”，不适合“带审计状态的正式消费层”。

#### 如果改选“物化视图”，会带来的限制

1. preview 与 official 难以共存。
2. blocked 行不自然。
3. `stats_snapshot_at`、回放批次、补跑责任链不够清晰。
4. 按单年、单 city、单 entity 局部重算不如表灵活。
5. 未来想保留多次快照时会再次返工。

#### 对第一阶段的结论

| 问题 | 最终建议 |
| --- | --- |
| 主方案选什么 | 表 |
| 第一阶段是否必须如此 | 是 |
| 是否允许后续另加视图作为查询层 | 可以，但那是第二层封装，不是主存储形态 |

#### 冻结表述

建议冻结为：

1. `gov_open_annual_stats_v2` 第一阶段采用正式结果表。
2. 若后续需要易查的聚合查询，可在其上再补只读 view，但不改变主物化形态。

### 3.3 `functional_zone / district / department / town_street` 的边界是否已经足够清晰

#### 最终建议

大边界已经足够清晰，可以实施。  
但必须补一份“类型判定优先级与特殊样本白名单”，并把它作为冻结补充单的一部分。

#### 必须再加的冻结附件

建议增加一个简版附件，内容只有两块：

1. 类型判定优先级表
2. 特殊样本白名单

#### 最容易误判的样本类别

| 类别 | 风险 |
| --- | --- |
| `新区` | 可能是行政区，也可能是功能区 |
| `高新区` | 可能是功能区本体，也可能是区内园区 |
| `开发区` | 本体与其下属部门容易混淆 |
| `园区` | 简称园区与功能区本体容易混淆 |
| `管委会` | 可能是功能区管理机构，也可能只是区内管理机构 |
| 括号复合名称 | 例如“高新区（如城街道、城南街道）” |
| 驻地机构 | 例如人民银行分行，不一定属于常规政府部门口径 |
| 农场/林场/原种场 | 不一定属于五大主类之一 |
| 截断名称/简称 | 例如“区医疗保障”“昆沭高新园” |

#### 是否建议作为冻结补充单的一部分

建议：是。

#### 第一阶段白名单范围是否要做很大

建议：否。  
第一阶段只做“小而关键”的白名单即可，后续再扩。

#### 冻结表述

建议冻结为：

1. 边界规则本轮足够清晰，可以实施。
2. 第一阶段必须同时落一份“类型判定优先级 + 特殊白名单”。
3. 白名单先覆盖高风险样本，不要求一开始全量覆盖所有边界名称。

### 3.4 official 版本选择规则是否已经可以冻结

#### 最终建议

可以，且必须直接冻结成固定决策表。

#### 决策表

| 场景 | `source_report_version_id` 选择 | `materialize_status` | 是否进入 official |
| --- | --- | --- | --- |
| `active_version.review_status = 'published'` | 取 `active_version_id` | `official` | 是 |
| `active_version.review_status = 'pending_review'`，且存在 published 版本 | 取“最新 published 版本” | `official` | 是 |
| `active_version.review_status = 'pending_review'`，且不存在 published 版本 | 取 active version | `preview` | 否 |
| facts 缺失 | 不产出正式统计 | `blocked_missing_facts` | 否 |
| mapping 未定 | 不产出正式统计 | `blocked_mapping_pending` | 否 |
| `unit_type = 'unknown'` | 不产出正式统计 | `blocked_unknown_unit_type` | 否 |

#### 为什么必须现在冻结

1. 这是第一阶段最核心的“正式/非正式”分界。
2. 如果开发边做边定，后续每次补跑都可能算出不同结果。
3. 这张表一旦定死，worker、rules service、报告生成和审计日志都能直接按同一口径实现。

#### 冻结表述

建议冻结为：

1. official 只接受 published 版本。
2. 没有 published 版本时，最多进入 preview，不得进入 official。
3. `facts`、`mapping`、`unit_type` 任一关键条件不满足时，不做 preview 替代，而是直接 blocked。

### 3.5 `unknown` 是否允许进入 preview

#### 最终建议

明确建议：**不允许**。

#### 明确回答

| 问题 | 最终建议 |
| --- | --- |
| `unknown` 是否允许进入 official | 不允许 |
| `unknown` 是否允许进入 preview | 不允许进入任何用户可见 preview |
| `unknown` 应进入哪里 | 仅进入内部治理清单、blocked 结果或映射待确认队列 |

#### 对各类消费场景的处理

| 场景 | 处理方式 |
| --- | --- |
| 主报告 | 不进入 |
| 区县/部门/功能区/镇街监测表 | 不进入 |
| 重点单位画像 | 不生成 |
| 领导驾驶舱 | 不进入正式看板；可单独显示“待确认单位数” |
| 内部治理台账 | 进入 |

#### 为什么不建议让 `unknown` 进 preview

1. preview 的意义应该是“未发布但结构正确的数据预览”，不是“单位类型尚未确定的数据试算”。
2. 一旦 `unknown` 进入 preview，前端和业务侧会把它视为“差不多能用了”，反而稀释治理动作。
3. `unknown` 的问题不是数据晚一点，而是口径根本未确定。

### 3.6 `city_region_id` 是否必须成为正式底座字段

#### 最终建议

必须。  
而且第一阶段就必须同时落在：

1. `canonical_units`
2. `gov_open_annual_stats_v2`

#### 明确回答

| 问题 | 最终建议 |
| --- | --- |
| 是否必须纳入 `canonical_units` | 是 |
| 是否必须纳入 `stats_v2` | 是 |
| 是否第一阶段就必须落 | 是 |

#### 如果暂不做，会带来的问题

| 场景 | 影响 |
| --- | --- |
| 主报告 | 市级汇总与分层对比要反复递归求归属地市 |
| 区县表 | 无法直接按所属市分组 |
| 部门表 | 同一套部门数据会在 city/district/function zone 归属上反复推导 |
| 驾驶舱 | 每个接口都要做单独归属计算，极易口径漂移 |

#### 冻结表述

建议冻结为：

1. `city_region_id` 为正式底座必备字段。
2. 所有非 province 实体都必须能解析出稳定 `city_region_id`。

---

## 四、首批人工映射名单建议

### 4.1 首批必须人工确认的样本类型

建议第一阶段优先人工确认以下样本类型：

1. `新区`
2. `高新区`
3. `开发区`
4. `园区`
5. `管委会`
6. 括号复合名称
7. 驻地机构
8. 农场/林场/原种场
9. 截断名称
10. 简称单位
11. 直辖市特殊样本

### 4.2 风险样本规模建议

基于当前真实库的边界模式粗筛，建议将以下范围视为第一阶段人工确认池：

1. 命中高风险模式的实体共约 89 个。
2. 其中已有 report 的约 72 个，应优先进入首批人工映射。
3. 另有 5 个低置信 `unknown`/疑难实体，必须全部纳入。

因此第一阶段最合适的人工范围不是 1909 个全量实体，而是：

1. 72 个“有报告的高风险边界实体”
2. 5 个“低置信/未知实体”
3. 少量“直辖市特殊白名单”

整体规模约 75-80 个，成本可控，且足以避免第一阶段返工。

### 4.3 首批建议纳入人工映射的实体名单

| region_id | org_name | 当前判断 | 为什么应人工确认 | 若不确认的风险 |
| ---: | --- | --- | --- | --- |
| 1437 | 浦东新区 | `district` | `新区` 类中的直辖市特殊样本，应固定为行政区而非功能区 | 会被 `新区` 规则误压成 `functional_zone` |
| 781 | 湖滨新区 | `functional_zone` | `新区` 名称但父级为市级，且下挂部门/街道，建议锁定为功能区 | 与行政区“新区”混判 |
| 782 | 洋河新区 | `functional_zone` | 同上 | 同上 |
| 1549 | 菊园新区 | 待人工确认 | `新区` 挂在区下，可能是功能区，也可能是街镇型特殊实体 | 误判后会影响区内层级聚合 |
| 2182 | 南通市经济技术开发区管委会 | `functional_zone` | 名称既像功能区又带 `管委会`，必须锁定本体语义 | 会被错归 `city` 或 `department` |
| 1312 | 清江浦经济开发区管委会 | 待人工确认 | `管委会` 与开发区组合，需确认是功能区本体还是区内机构 | 区县/功能区口径混乱 |
| 2547 | 南京市秦淮区新街口管委会 | 待人工确认 | `管委会` 但未必是标准开发区，需要业务侧定性 | 误判会影响重点单位画像和排序 |
| 2268 | 高新区（如城街道、城南街道） | `functional_zone` | 典型复合名称，需人工确认按功能区整体处理 | 街道与功能区双重语义冲突 |
| 2489 | 海门经济技术开发区(海门街道) | `functional_zone` | 功能区与街道括号复合，需锁定主语义 | 容易拆错父级和单位类型 |
| 2635 | 南京徐庄高新区技术产业开发区管理委员会 | `functional_zone` | 长复合名称，高风险边界样本 | 容易被规则拆错为 `department` |
| 770 | 人民银行宿迁市分行 | `department`（建议） | 驻地机构，不一定属于常规政府部门口径 | 是否进入正式部门分析不明确 |
| 856 | 昆沭高新园 | 待人工确认 | 简称实体，自动规则不稳定 | 可能漏判或错判为 unknown |
| 1084 | 泗阳棉花原种场 | 待人工确认 | 场类样本，未纳入正式五类主型 | 未来要么错分，要么永远挂 unknown |
| 1085 | 泗阳农场 | 待人工确认 | 同上 | 同上 |
| 1108 | 湖滨新区嶂山林场 | 待人工确认 | 林场类样本，边界不清 | 功能区下属特殊单位可能误入主分析 |
| 2434 | 区医疗保障 | `department`（建议） | 名称明显截断，必须人工确认真实机构名 | 会把错误名称固化进正式底座 |

### 4.4 首批人工映射范围建议

#### 最终建议

不建议全量人工过一遍。  
建议只覆盖：

1. 全部低置信/未知实体
2. 全部“有报告”的高风险模式实体
3. 全部直辖市特殊白名单样本

#### 为什么这是最优成本方案

1. 第一阶段真正影响正式报告和 `stats_v2` 的，是“会被消费”的高风险实体，而不是所有树节点。
2. 先覆盖约 75-80 个高风险实体，足以挡住主要返工点。
3. 其余普通部门、普通区县、普通镇街不值得在开工前做人工全检。

#### 冻结表述

建议冻结为：

1. 第一阶段不做全量人工 mapping。
2. 第一阶段人工 mapping 范围以“高风险且影响消费的实体”为准。
3. 无 report 的风险实体可以先进入待办池，不阻塞底座开工。

---

## 五、可冻结项 / 暂缓项二分表

### 5.1 A 类：现在必须冻结，否则不能开工

| 设计点 | 是否必须现在冻结 | 最终建议 |
| --- | --- | --- |
| `unit_type` 枚举 | 是 | 本轮冻结，不再扩类 |
| `parent_region_id` 规则 | 是 | 默认源自 `regions.parent_id`，最终以 `canonical_units` 为准 |
| `city_region_id` | 是 | 第一阶段必须落入 `canonical_units` 和 `stats_v2` |
| `year` 口径 | 是 | 唯一来源 `reports.year` |
| official / preview / blocked 规则 | 是 | 本轮直接冻结决策表 |
| `canonical_units` 结构边界 | 是 | 本轮冻结最小字段集 |
| `stats_v2` 形态 | 是 | 第一阶段采用表 |
| 样本白名单 | 是 | 需要冻结一份小型白名单 |
| 人工映射机制 | 是 | 冻结“先高风险、后扩展”的策略 |
| 新旧口径对账方式 | 是 | 冻结三层对账方法 |
| `unknown` 治理规则 | 是 | 仅 blocked/治理清单，不进用户可见结果 |

### 5.2 B 类：可以暂缓到第二阶段，不影响第一阶段启动

| 设计点 | 是否可暂缓 | 暂缓理由 |
| --- | --- | --- |
| 历史组织变更的真实启用方式 | 是 | 先预留字段即可，第一阶段不需要启用多时态映射 |
| 驾驶舱联动改造 | 是 | 先把字段准备好，UI 切换可后置 |
| 区县/部门正式接入主报告正文 | 是 | 第一阶段先把底座和规则层跑通 |
| 白名单的持续扩展机制 | 是 | 第一阶段只需小而关键的首批白名单 |
| 更细的子类型扩类 | 是 | 第一阶段不新增 `stationed_agency/farm_forest_field` 等子类 |
| 全量无报告实体人工确认 | 是 | 不影响第一阶段正式消费 |

---

## 六、实施前冻结结论

### 6.1 对“是否可以开工”的最终回答

可以。  
当前底座方案已经可以视为“冻结后开工”的基础。

### 6.2 如果还差一点，差的到底是什么

严格说，不差新的方案设计，只差以下事项被正式拍板：

1. `stats_v2` 采用表
2. `city_region_id` 第一阶段强制纳入
3. official / preview / blocked 决策表正式确认
4. `unknown` 不进入用户可见 preview
5. 首批人工映射范围按“约 75-80 个高风险实体”执行

### 6.3 是否只需要补这一页冻结补充单，就可以进入第一阶段实施

是。  
从当前状态看，不需要再追加一轮大范围摸底。  
只需要把本补充单作为“冻结附件”挂到现有底座方案后面，即可进入第一阶段实施任务拆解。

### 6.4 第一阶段开工前必须再确认的 3-5 个事项

建议最终只确认以下 5 个事项：

1. `gov_open_annual_stats_v2` 主形态是否按“表”落地
2. `city_region_id` 是否同时进入 `canonical_units` 和 `stats_v2`
3. official / preview / blocked 决策表是否按本补充单执行
4. `unknown` 是否彻底排除在所有用户可见结果之外
5. 首批人工映射是否按“72 个有报告高风险实体 + 低置信样本 + 直辖市特殊白名单”执行

### 6.5 是否建议现在进入“第一阶段实施任务清单”编制阶段

建议进入。  
而且建议不要再拖到下一轮调研之后再编制，否则会重新回到“边做边定口径”的状态。

### 6.6 最终一句话结论

底座方案已经具备开工条件；本补充单应作为最终冻结附件使用，确认后即可进入第一阶段实施任务清单编制与排期。

