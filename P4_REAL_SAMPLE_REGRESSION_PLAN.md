# P4-1 真实样本全链路回归与问题清单计划

## 1. 阶段目标

P4-1 只做真实/准真实样本的端到端回归、证据采集和问题清单，不做功能开发、不做架构重构、不改数据库 schema。目标是确认从报告上传、解析、人工复核、比对、问题定位、PDF 导出、任务中心、GovInsight 报告到权限边界的主业务链路是否已经具备进入交付稳定化后续阶段的基础。

本阶段输出应回答四个问题：

1. 主链路在当前 `main` 基线下是否可复现、可演示、可复核。
2. 各类真实样本是否有明确的通过/失败证据。
3. 阻塞客户演示或验收的问题有哪些，严重级别是什么。
4. 是否建议进入 P4-2 数据质量与证据链增强。

## 2. 当前 main commit

- 当前分支基线：`main`
- 当前 main commit：`17667cdec3e1c5d1feabcf37a83f3f0844f9eb5c`
- 简短提交：`17667cd Merge pull request #106 from zxj6827111-blip/codex/p3-final-ux-polish`
- P4-1 工作分支：`codex/p4-real-sample-regression`

## 3. 测试环境假设

P4-1 回归默认使用本地可控环境，必要时补充准生产环境复核。

环境假设：

1. Node.js 满足仓库要求 `>=20.0.0`。
2. 根目录依赖和 `frontend` 依赖已安装，且不在 P4-1 中升级依赖。
3. 后端测试服务可使用 `http://127.0.0.1:8787`。
4. 前端测试服务可使用 `http://127.0.0.1:3001`。
5. PostgreSQL 测试库可连接，包含或可导入 P4-1 所需真实/准真实样本。
6. PDF 导出所需 Chromium/Puppeteer 运行环境可用。
7. 可用测试账号至少包含管理员账号、普通业务账号、受限权限账号。
8. P4-1 不默认连接生产库，不直接修改生产数据。
9. 若需要上传真实文件，文件来源、脱敏状态和使用边界需人工确认。
10. 失败任务、过期文件、批量下载等样本优先使用测试库或可恢复的准真实数据。

## 4. 样本分类

P4-1 样本按业务风险和验收价值分组，每组至少保留样本来源、样本 ID、预期结果、实际结果和证据链接。

| 分类 | 覆盖目标 | 建议样本口径 | 重点观察 |
| --- | --- | --- | --- |
| 普通年度报告 | 验证标准上传、解析、复核、比对主链路 | 区县或部门普通年度报告，表二/表三/表四结构完整 | 自动识别、解析完成、版本选择、比对结果、问题编号 |
| 长表格年度报告 | 验证长表格渲染、定位和 PDF 分页 | 表二或表三行数多、跨页明显的年度报告 | 页面滚动、定位、打印分页、PDF 无空白页 |
| 表三复杂样本 | 验证表三字段、行列映射和异常检测 | 表三存在多行、多列、申请处理分项复杂样本 | 单元格路径、问题 badge、focus banner、复核动作 |
| 缺表或异常样本 | 验证解析失败、缺表、不可评估、异常提示 | 缺少表二/三/四、标题异常、扫描质量差、PDF 文本弱样本 | 错误提示、失败状态、是否可重试、是否误判成功 |
| GovInsight 城市样本 | 验证城市智能报告与 PDF 导出 | 例如 `city_721/2025` 及 numeric orgId 兼容样本 | 报告生成、目录、图表稳定、`/api/gov-insight/report-pdf` |
| PDF 导出样本 | 验证 comparison PDF 和 GovInsight PDF | 普通 comparison、长表格 comparison、GovInsight report | `/api/pdf-jobs`、`/api/comparisons/:id/pdf`、print route |
| 批量下载样本 | 验证多个 PDF 任务打包下载 | 多个 done PDF job，混合普通和长表格 | ZIP 内容、文件名、失败项处理、权限 |
| 失败任务/过期文件样本 | 验证任务中心异常处理 | failed job、file_path 缺失、过期文件、可重试任务 | 下载错误、重新生成、状态刷新、用户提示 |
| 权限边界样本 | 验证认证、授权和只读/受限账号 | admin、业务账号、无上传权限、无任务管理权限账号 | API 401/403、菜单可见性、越权操作拦截 |

历史高价值回归锚点可作为优先候选，但必须以当前测试库实际存在为准：

- `report_id=3670` / `version_id=3448`：报告详情、表格问题定位、复核动作候选。
- `report_id=4304` / `version_id=3964`：表四稳定候选。
- `report_id=4837` / `version_id=4403`：OCR/vision review 异常候选。
- `report_id=4839` / `version_id=4405`：source table anomaly 候选。
- `comparison_id=4670`：PDF smoke 默认普通 comparison 候选。
- `comparison_id=1143`：PDF smoke 默认长表格 comparison 候选。
- `orgId=city_721` / `year=2025`：GovInsight PDF 默认候选。

## 5. 测试账号和权限

P4-1 需要人工确认测试账号，不在计划阶段写入密码、令牌或敏感信息。

| 账号类型 | 权限要求 | 验证重点 |
| --- | --- | --- |
| 管理员账号 | 用户管理、城市管理、查看报告、上传报告、比对报告、任务管理 | 完整链路、用户管理入口、批量下载和重试 |
| 业务操作账号 | 查看报告、上传报告、比对报告 | 上传、解析、复核、比对、导出 |
| 只读账号 | 查看报告 | 报告浏览、问题定位、禁止上传/删除/重试 |
| 受限账号 | 缺少至少一项关键权限 | 菜单隐藏、API 403、禁止越权操作 |
| 未登录状态 | 无 token | 登录跳转、API 401、print/PDF token 处理 |

权限验证必须同时覆盖前端可见状态和后端 API 返回，不能只看按钮是否显示。

## 6. 完整业务链路

P4-1 回归按以下链路逐段记录，不要求每个样本都覆盖全部链路，但每条关键链路至少有一个通过样本和一个异常/边界样本。

1. 登录与会话：登录、刷新、退出、无效 token、权限加载。
2. 区域/城市数据：地区树加载、目标地区选择、GovInsight 城市选择。
3. 单文件上传：选择 PDF、首页识别、年份/地区回填、上传并触发解析。
4. 批量上传：批量文件选择、任务创建、进度展示、失败项提示。
5. 解析任务：任务中心状态流转、成功/失败详情、重试入口。
6. 报告详情：报告元数据、版本选择、表二/表三/表四展示、异常提示。
7. 质量检查：问题摘要、编号体系、badge、focus banner、定位到表格单元格。
8. 人工复核：确认、驳回、重置、批量确认等状态动作及刷新后持久性。
9. 年度比对：创建 comparison、查看 comparison history、进入详情。
10. 比对结果：差异摘要、表格差异、数据质量问题、证据可追溯。
11. comparison PDF：通过 `/api/pdf-jobs` 创建任务、下载、重新生成、批量下载。
12. 同步兼容 PDF：验证 `/api/comparisons/:id/pdf` 不破坏。
13. print 页面：验证 `/print/comparison/:id` 的 print-ready 状态和页面完整性。
14. GovInsight 报告：生成/查看城市报告，验证 `/govinsight` 主路径。
15. GovInsight PDF：验证 `/api/gov-insight/report-pdf` 和 `/print/govinsight-report/:orgId/:year`。
16. 失败/过期任务：下载失败、文件缺失、过期、重新生成、清理提示。
17. 权限边界：不同账号对上传、比对、导出、任务管理、用户管理的访问控制。

## 7. 每一步记录口径

每个测试步骤统一记录以下字段，便于复核和后续 P4-2 证据链增强。

| 字段 | 说明 |
| --- | --- |
| 用例编号 | 例如 `P4-1-UPLOAD-001`、`P4-1-PDF-003` |
| 样本分类 | 对应第 4 节分类 |
| 样本标识 | report_id、version_id、comparison_id、orgId/year、job_id 或文件名 |
| 账号类型 | 管理员、业务、只读、受限、未登录 |
| 操作入口 | 页面路径、按钮、API 路径或脚本命令 |
| 前置条件 | 服务端口、数据库状态、文件是否存在、任务状态 |
| 预期结果 | 可观察的业务结果，不只写“成功” |
| 实际结果 | 页面/API/脚本的真实返回 |
| 证据 | 截图、日志片段、API 响应摘要、PDF 文件 hash、job_id |
| 结论 | Pass、Fail、Blocked、Skipped |
| 问题编号 | 若失败，关联 P4-1 问题清单编号 |
| 清理动作 | 是否恢复 pending、删除临时任务、移除临时文件 |

记录原则：

1. 页面验证要写清楚可见结果，例如编号、状态、banner、按钮、下载文件名。
2. API 验证要写清楚 HTTP 状态、关键字段和错误码。
3. PDF 验证要写清楚页数、空白页、关键文本、文件大小或 hash。
4. 人工复核动作要记录操作前后状态，并刷新页面后复核。
5. 临时样本必须记录 cleanup 结果。

## 8. 自动验证基线

P4-1 开始真实样本回归前，先运行以下自动验证基线。命令只作为计划写入，本计划阶段不执行。

```powershell
npm.cmd run build
npm.cmd test
cd frontend && npm.cmd test -- --runInBand
cd frontend && npm.cmd run build
npm.cmd run smoke:pdf
npm.cmd run smoke:pdf -- --strict-live --api-base=http://127.0.0.1:8787 --frontend-url=http://127.0.0.1:3001
```

基线判定：

1. `npm.cmd run build` 必须通过，确认后端 TypeScript 和 public 资源构建正常。
2. `npm.cmd test` 必须通过，确认后端回归测试稳定。
3. `frontend` Jest 必须通过，确认 P3 AppShell、路由、TaskDrawer、上传识别、报告详情等前端回归不退化。
4. `frontend` build 必须通过，确认 React Router、CSS 架构和生产构建正常。
5. 默认 `smoke:pdf` 必须通过；若 live 检查被 skipped，必须在报告中单独标注。
6. `--strict-live` smoke 必须在后端 `8787` 和前端 `3001` 都可访问时通过；任何 skipped、failed 或端口不可用都视为 P4-1 前置阻塞。
7. 不允许为通过 P4-1 基线而修改 `scripts/pdf-smoke-baseline.js`。

## 9. 人工验证清单

人工验证按样本和链路拆分，优先覆盖客户演示最可能经过的路径。

| 编号 | 验证项 | 最低通过标准 |
| --- | --- | --- |
| M-01 | 登录和权限加载 | 不同账号能看到符合权限的菜单和操作入口 |
| M-02 | 普通年度报告上传 | 自动识别年份/地区，上传后产生可追踪任务 |
| M-03 | 长表格年度报告解析 | 报告详情能完整展示长表格，无明显错位 |
| M-04 | 表三复杂样本定位 | 问题编号、badge、focus banner、单元格高亮一致 |
| M-05 | 缺表/异常样本 | 显示可理解错误，不误报为完整成功 |
| M-06 | 人工复核动作 | 确认/驳回/重置后状态刷新仍一致 |
| M-07 | 创建年度比对 | comparison 创建成功并进入详情页 |
| M-08 | 比对详情阅读 | 摘要、表格、问题清单可读且证据可追溯 |
| M-09 | comparison PDF job | `/api/pdf-jobs` 创建、列表、下载、重生成可用 |
| M-10 | comparison 同步 PDF | `/api/comparisons/:id/pdf` 返回 PDF，不破坏兼容路径 |
| M-11 | comparison print 页面 | `/print/comparison/:id` 页面完整并出现 ready 标记 |
| M-12 | GovInsight 城市报告 | 城市报告内容、图表、目录和风险项可读 |
| M-13 | GovInsight PDF | `/api/gov-insight/report-pdf` 生成 PDF，print route 兼容 `city_721` |
| M-14 | 批量下载 | 多个 done PDF job 可打包，失败项不污染成功项 |
| M-15 | 失败/过期任务 | 下载、重试、删除或提示符合预期 |
| M-16 | 只读账号边界 | 只读账号不可上传、删除、重试、管理用户 |
| M-17 | 未登录边界 | 受保护 API 返回 401，页面进入登录流程 |
| M-18 | 刷新和深链 | 关键路由刷新后仍可恢复正确页面 |
| M-19 | 浏览器控制台 | 客户演示路径无新增红色错误 |
| M-20 | 清理复核 | 临时任务、临时文件和状态动作已恢复或记录保留原因 |

## 10. 问题分级标准

| 级别 | 定义 | 示例 | 处理建议 |
| --- | --- | --- | --- |
| P0 阻塞 | 主链路不可用、数据错误严重、权限越权、PDF 核心导出全失败 | 无法登录、报告无法上传、comparison 创建失败、未授权可删除数据 | 立即停止验收，单独开修复分支 |
| P1 高 | 关键链路可绕过但不稳定，影响客户演示或专家评审可信度 | 长表格 PDF 空白页、问题定位错位、GovInsight PDF 缺章节 | P4-1 记录并建议优先修复 |
| P2 中 | 局部体验或边界异常，不影响主链路完成但影响产品化 | 错误提示不清晰、任务状态刷新慢、少量 console warning | 进入 P4-2/P4-5 排期 |
| P3 低 | 文案、样式、非关键展示瑕疵 | 对齐轻微偏差、说明文字不统一 | 记录，随 UX polish 或验收材料阶段处理 |
| Blocked | 环境、样本、账号、依赖缺失导致不能判断 | 测试库缺样本、无权限账号、端口不可用 | 补齐前置条件后重测 |

所有问题必须包含复现步骤、样本标识、实际证据、期望结果、影响范围和建议归属阶段。

## 11. 输出物

P4-1 计划完成后，真实回归阶段应产出以下文件或记录：

1. `P4_REAL_SAMPLE_REGRESSION_PLAN.md`：本计划。
2. `P4_REAL_SAMPLE_REGRESSION_REPORT.md`：逐项回归结果、自动验证结果、人工验证结果。
3. `P4_REAL_SAMPLE_ISSUE_LIST.md`：问题清单、分级、复现步骤、证据、建议处理阶段。
4. `P4_REAL_SAMPLE_FIX_RECOMMENDATIONS.md`：若存在阻塞，列出最小修复建议和 non-goals。
5. 自动验证输出摘要：命令、时间、结果、失败日志关键片段。
6. 人工验证证据索引：截图、PDF hash、job_id、API 响应摘要。
7. cleanup 记录：临时 report/version/checks/runs/items/region/job/file 的保留或恢复情况。

P4-1 报告不得包含敏感密码、token、生产数据明细或未经脱敏的客户隐私信息。

## 12. 不改动清单

P4-1 当前阶段明确不做以下事项：

1. 不开发业务功能。
2. 不改前端组件、后端接口、服务层、数据库访问逻辑。
3. 不改数据库 schema，不运行 schema migration。
4. 不删除 legacy EJS。
5. 不删除旧接口。
6. 不破坏或重写 `/api/pdf-jobs`。
7. 不破坏或重写 `/api/comparisons/:id/pdf`。
8. 不破坏或重写 `/api/gov-insight/report-pdf`。
9. 不破坏或重写 `/print/comparison/:id`。
10. 不破坏或重写 `/print/govinsight-report/:orgId/:year`。
11. 不修改 `scripts/pdf-smoke-baseline.js`。
12. 不把 P4-1、P4-2、P4-3、P4-4、P4-5 混在同一分支。
13. 不提交 `node_modules`、`dist`、`build`、`coverage`、`logs`、PDF、ZIP、截图、HTML dump。
14. 不使用 `git add .`。
15. 不提交、不 push、不 merge，除非后续获得人工确认。

## 13. 风险与前置条件

当前能否顺利开始 P4-1，取决于以下前置条件：

1. 测试库必须存在足够样本，或能安全导入准真实样本。
2. 测试账号和权限矩阵必须可用，尤其是受限权限账号。
3. 后端 `8787` 和前端 `3001` 服务必须能稳定启动。
4. PDF 导出运行环境必须可用，包括 Puppeteer/Chromium 和字体环境。
5. `smoke:pdf --strict-live` 依赖 live API 和前端页面，服务不可用时不能用默认 skipped 结果替代。
6. 上传和复核类用例可能产生临时数据，必须提前约定 cleanup 方式。
7. 真实样本可能包含敏感信息，必须先确认脱敏和使用范围。
8. 已知 `main` 工作区存在未跟踪的 P3 报告文件，P4-1 不处理、不暂存，后续提交前需要保持边界清洁。
9. 若发现 P0/P1 问题，应停止 P4-1 验收扩展，单独开修复分支处理。
10. 若需要修改业务代码，必须先形成问题报告和最小修复方案，等待人工确认。

## 14. 是否建议开始真实样本回归

建议开始 P4-1 的准备性回归，但正式进入真实样本全链路验收前，应先补齐并确认以下事项：

1. 人工确认测试账号和权限矩阵。
2. 人工确认真实/准真实样本清单及脱敏边界。
3. 启动后端 `http://127.0.0.1:8787` 和前端 `http://127.0.0.1:3001`。
4. 先运行第 8 节自动验证基线，尤其是 `smoke:pdf --strict-live`。
5. 若自动验证基线全绿，再按第 9 节人工验证清单执行真实样本回归。

当前计划阶段未发现必须阻止创建 P4-1 分支或编写计划的问题；但在自动验证基线和样本/账号确认完成前，不建议宣称 P4-1 已正式通过验收。
