# P4 客户演示脚本

## 1. 演示目标

本脚本用于 P4 客户演示和专家评审。演示重点是让客户理解系统如何把政府信息公开年报从上传、解析、复核、发布、比对、证据链查看、PDF 导出到 GovInsight 分析串成可复核流程。

演示不新增业务能力，不现场修改代码、不改数据库、不改部署脚本、不触发高风险数据修复。现场只展示已验证路径和已准备样本。

## 2. 演示前准备

演示前应确认：

- 后端 API 可用：建议 `http://127.0.0.1:8787`。
- 前端可用：建议 `http://127.0.0.1:3001`。
- 登录账号可用，且明确账号权限和数据范围。
- 推荐样本已存在，避免现场临时找数据。
- `npm.cmd run smoke:pdf` 已执行通过。
- 如 API 和前端均可用，strict-live 已执行通过；如未启动，记录 blocked 原因。
- 浏览器中不展示真实 `.env`、secret、token、数据库密码或敏感文件路径。
- 若需展示 PDF，提前确认文件可下载或可重新生成。
- 若需展示 TaskDrawer，提前人工登录并确认入口可见。

建议准备两个浏览器标签：

- 标签 1：业务前端，登录后打开 `/catalog`。
- 标签 2：任务中心，打开 `/jobs` 或 `/jobs?tab=download`。

## 3. 演示数据要求

推荐使用已在 P4-1/P4-2/P4-3 验证过的样本：

| 样本 | 推荐用途 |
| --- | --- |
| report `4691` / version `4314` | 上传、解析复用、复核、发布说明 |
| report `4837` / version `4457` | 复杂表格、数据质量说明 |
| report `4839` / version `4405` | 缺表/异常样本、发布阻断说明 |
| comparison `4670` | 淮安市 2024-2025 比对、PDF 导出、异常状态 |
| comparison `1143` | 长表格比对、PDF 版式样本 |
| `city_721` / `721`, year `2025` | GovInsight 已存报告和 PDF |
| failed PDF job `18396` | PDF 未完成/失败下载兜底说明 |
| expired PDF job `18278` | 文件过期和重新生成说明 |

不建议现场临时上传大批量未知样本；如客户要求，可说明“现场演示使用已验证样本，新增地区和年份应按验收流程先跑解析与复核”。

## 4. 5 分钟领导演示版

目标：讲清楚系统价值和闭环，不展开技术细节。

建议节奏：

1. 登录进入系统，展示年报工作台。
2. 打开一个已发布报告，说明系统把年报结构化，并保留复核入口。
3. 打开比对中心，进入 comparison `4670` 或 `1143`。
4. 展示差异摘要、关键差异和数据质量提示。
5. 打开 PDF 导出入口或已生成 PDF，说明报告可归档、可流转。
6. 切到 GovInsight，展示 `city_721` 2025 已存分析报告和打印视图。
7. 打开任务中心，说明上传解析和 PDF 导出都有状态跟踪。
8. 结论话术：系统不是替代人工签字，而是把年报入库、比对、复核、证据和输出统一到一条可追踪链路中。

领导版推荐话术：

- “这套系统把年报从文件变成可复核的数据资产。”
- “差异不是只给结论，还能回到字段、风险提示和人工复核入口。”
- “PDF 和 GovInsight 用于汇报归档，任务中心用于交付过程可追踪。”
- “异常样本不会静默通过，会进入复核或阻断。”

避免展开：

- 不现场讲 schema、migration、算法细节。
- 不现场承诺压测指标或安全加固完成。
- 不现场重新生成 GovInsight latest job。

## 5. 15 分钟专家评审版

目标：让专家看到可复核性、边界和证据链。

建议节奏：

1. 登录和权限边界：说明业务接口需要 token，管理入口受权限键控制，数据范围按地区约束。
2. 年报工作台：打开 `/catalog`，定位报告和年份。
3. 报告详情：打开 report `3670`、`4691` 或 `4837`，展示勾稽检查、问题项和“证据说明”。
4. 异常样本：打开 report `4839` 的说明材料，讲发布阻断和 open issue。
5. 比对详情：打开 comparison `4670`，说明差异来自已入库报告，不是临时上传即比对旧链路。
6. 长表格样本：打开 comparison `1143`，说明长表格 PDF 已纳入 smoke。
7. PDF 导出：展示 `/jobs?tab=download`，说明完成、生成中、失败、过期和批量下载边界。
8. GovInsight：展示 `city_721` 2025 的已存报告、来源状态和 PDF；明确 latest job `17` failed 是已知遗留问题，现场不作为重新生成演示项。
9. 运维材料：引用 `DEPLOYMENT.md`、`OPERATIONS.md`、`TROUBLESHOOTING.md`，说明健康检查、诊断、备份恢复和回滚。
10. 总结边界：P4-4 是验收与演示材料，不混入 P4-5 性能安全加固。

专家版推荐话术：

- “系统检查给出机器初筛和证据线索，最终确认仍由人工复核承担。”
- “没有来源时不会虚构页码或 OCR 原文，只保留结构化字段路径和保守风险提示。”
- “PDF smoke 当前在本机可做文本、页数、空白页和 ready marker 检查；像素级能力取决于 Poppler、ImageMagick、Ghostscript 是否安装。”
- “GovInsight 当前可演示已存报告和 PDF，不建议现场演示 latest job 重新生成。”

## 6. 完整业务流程演示版

完整流程建议控制在 30-45 分钟，按以下路径执行：

1. 登录。
2. 年报工作台查看地区、年份和报告列表。
3. 年报维护或上传入口说明。
4. 上传报告并创建解析任务，或使用已验证 report `4691` 说明复用结果。
5. 任务中心查看解析任务状态。
6. 报告详情查看结构化结果。
7. 勾稽关系页查看检查项、证据说明和定位。
8. 异常样本说明发布阻断。
9. 发布后的报告进入比对中心。
10. 比对历史进入 comparison `4670` 或 `1143`。
11. 查看差异摘要、差异来源说明和关键字段。
12. 创建或展示 PDF 导出任务。
13. 任务中心下载 tab 查看 PDF 状态、下载、过期和批量下载。
14. 打开 GovInsight，展示城市报告、来源状态和 PDF 打印页。
15. 引用运维手册说明部署、健康检查、诊断、备份恢复和回滚。
16. 回到验收标准，说明通过条件和已知遗留项。

## 7. 年报上传/解析/复核/发布演示

演示路径：

1. 进入 `/upload`。
2. 选择地区、年份和单位，上传年度报告 PDF。
3. 提交后说明系统创建解析任务，TaskDrawer 或任务中心显示状态。
4. 进入 `/jobs` 查看解析任务排队、处理中、成功或失败。
5. 解析成功后进入报告详情。
6. 查看勾稽检查和证据说明。
7. 对无阻断问题样本说明发布路径。
8. 对 report `4839` 这类异常样本说明 open issue 阻断发布。

推荐话术：

- “上传后不是直接进入比对，而是先入库、解析、复核，再发布为可比对报告。”
- “异常样本不会被强行发布，系统会把风险暴露给人工处理。”

演示注意：

- 现场如不需要制造新数据，优先使用已存在 report `4691`、`4837`、`4839`。
- 如果上传失败，切到已验证样本继续演示，并说明失败原因需要看任务中心错误详情。

## 8. 年报比对演示

演示路径：

1. 进入 `/history`。
2. 打开 comparison `4670`。
3. 说明地区、年份、相似度和检查状态。
4. 展示章节差异、表格差异或差异来源说明。
5. 打开 comparison `1143` 展示长表格比对样本。
6. 说明系统当前使用 report-based compare 主线，旧版 `/api/v1/tasks/compare/*` 已作为 retired 防线，不作为可用业务入口。

推荐话术：

- “比对基于已发布、可复核的报告，避免直接拿两个临时文件做不可追踪比对。”
- “差异结果用于辅助复核，不替代人工最终判断。”

## 9. 数据质量与证据链演示

演示路径：

1. 打开报告详情页，进入勾稽关系或问题项。
2. 展示 `FAIL`、`UNCERTAIN`、`NOT_ASSESSABLE` 项。
3. 展示“证据说明”中的字段路径、原始值、解析值、比对值、风险原因。
4. 打开 `/comparison/4670`，展示差异来源说明。
5. 打开 GovInsight 打印页，展示来源状态。

推荐话术：

- “系统尽量展示已有来源和字段路径；没有更详细来源时不编造证据。”
- “数据质量提示是复核线索，不是自动定责结论。”

## 10. PDF 导出演示

演示路径：

1. 在比对详情中触发 PDF 导出，或展示已有 job `18473`。
2. 打开 `/jobs?tab=download`。
3. 展示可下载、生成中、失败、已过期四类状态。
4. 下载已完成且文件存在的 PDF。
5. 展示过期文件的重新生成入口。
6. 选择多个已完成且未过期任务，说明批量下载只打包可下载文件。

推荐话术：

- “PDF 推荐走任务化导出，便于排队、重试、下载和过期处理。”
- “过期文件不会伪装成成功下载，而是提示重新生成。”

## 11. GovInsight 专项演示

演示路径：

1. 进入 `/govinsight`。
2. 选择淮安市 2025，展示已有分析视图。
3. 打开 `/print/govinsight-report/city_721/2025`。
4. 展示来源状态：已保存 payload、预览/辅助口径、source job、source report version、dataQuality。
5. 打开 PDF 输出或说明 `/api/gov-insight/report-pdf?org_id=city_721&year=2025` 已验证可用。
6. 如专家追问 latest job，明确 latest job `17` failed 是已知遗留问题，当前演示使用历史成功 job `16` 的已存报告和 PDF。

推荐话术：

- “GovInsight 的演示重点是已存报告、来源状态和可归档 PDF。”
- “重新生成链路的异常不会在本阶段掩盖，已纳入遗留问题边界。”

## 12. TaskDrawer 与任务中心演示

演示路径：

1. 在 `/upload` 触发或说明解析任务，观察 TaskDrawer 入口。
2. 打开 `/jobs` 查看上传解析任务。
3. 在比对详情触发或说明 PDF 导出任务。
4. 打开 `/jobs?tab=download` 查看 PDF 下载任务。
5. 展示任务状态刷新、失败、过期、重新生成和批量下载。

推荐话术：

- “TaskDrawer 是当前业务页面内的任务观察入口，任务中心是集中管理入口。”
- “如果浏览器自动化登录受限，应按 `OPERATIONS.md` 用真实账号人工补一次可视化验收。”

## 13. 演示失败时的兜底话术

| 场景 | 兜底处理 | 推荐话术 |
| --- | --- | --- |
| 登录失败 | 检查账号、密码、token 过期、API 地址 | “先切换到已登录会话或接口证据，登录问题按故障手册排查。” |
| API 不通 | 打开健康检查或运维材料 | “这是环境联通问题，不直接等同于业务功能失败，按 `ops:health` 定位。” |
| 前端不通 | 切到 API 或截图材料 | “前端服务未启动时不做 strict-live，通过 blocked 记录环境原因。” |
| 上传失败 | 切换到已验证 report 样本 | “现场新增样本失败不影响已验证样本链路，失败原因进入任务中心复核。” |
| 解析失败 | 展示任务中心错误 | “解析失败是可见状态，需由人工判断样本问题还是服务问题。” |
| PDF 未完成 | 展示下载任务状态 | “PDF 任务未完成不会提供文件，完成后可下载。” |
| PDF 过期 | 展示重新生成 | “过期文件需要重新生成，这是受控行为。” |
| GovInsight latest job 失败 | 展示已存报告和 PDF | “当前已存报告和 PDF 可用，latest job 失败是已知遗留问题，不现场承诺已修复。” |
| TaskDrawer 不可见 | 切到任务中心 | “页面内抽屉和任务中心都服务于任务追踪，人工验收可按运维手册补做。” |

## 14. 不建议现场演示的高风险项

以下内容不建议在客户现场临时演示：

- 大批量未知 PDF 上传和解析。
- GovInsight latest job 重新生成。
- 数据库迁移、schema 修改或修复脚本。
- 部署脚本、备份恢复、回滚的真实执行。
- 性能压测、安全扫描、权限模型改造。
- 删除任务、删除 PDF 文件、清理上传目录或清理备份。
- legacy EJS 作为推荐 PDF 主路径。
- 手工修改 `.env`、token、secret。

如客户要求查看，应使用文档、只读检查或已验证证据说明，不在演示现场做破坏性或不可逆操作。

## 15. 演示收尾建议

收尾时按以下顺序总结：

1. 主链路已形成：年报上传、解析、复核、发布、比对、PDF、GovInsight。
2. 证据链已补齐到可复核层面：字段路径、风险原因、来源状态。
3. 任务可追踪：解析任务、PDF 导出任务、失败和过期状态。
4. 运维可交付：部署、巡检、诊断、备份恢复和回滚材料已存在。
5. 边界清晰：性能安全加固、GovInsight latest job 根因修复、PDF 像素级工具链等不写成本阶段已完成。
